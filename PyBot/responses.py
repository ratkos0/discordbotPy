"""def handle_response(message) -> str:
    p_message = message.lower()

    if p_message == 'test':
        return 'Bot funkcionalan'
    
    if p_message == 'help':
        return "Opcije pomoc nisu dostupne trenutno"
    """
